# Backend API URL — always the production Render instance.
# This is the same for local development AND production (Vercel).
# Never change this to localhost — the Render backend is always used.
VITE_API_URL=https://coderv-backend.onrender.com
